# Демо-дані для презентації

Цей документ описує як заповнити базу демонстраційними даними для тестування / показу роботи Hand&Hand.

---

## Запуск seed

Скрипт лежить у `prisma/seed.sql`. Виконується через Prisma CLI (бере `DATABASE_URL` з `.env` автоматично):

```powershell
# з кореня репозиторію
npx prisma db execute --file prisma/seed.sql
```

> **Перед першим запуском** треба синхронізувати схему з БД:
>
> ```powershell
> npx prisma db push
> ```
>
> Якщо БД у поганому стані — `npx prisma migrate reset` (⚠️ видалить дані).

Скрипт повністю **ідемпотентний**: усередині `TRUNCATE ... RESTART IDENTITY CASCADE`, тож можна перезапускати скільки завгодно — попередні демо-дані затруться, sequences скинуться.

### Альтернативи (якщо `prisma db execute` не працює)

```powershell
# через psql напряму (якщо PostgreSQL у PATH):
psql "$env:DATABASE_URL" -f prisma/seed.sql

# через повний шлях до psql.exe (якщо не в PATH):
& "<path-to-postgresql>\bin\psql.exe" "$env:DATABASE_URL" -f prisma/seed.sql
```

---

## Демо-логіни

**Пароль для всіх користувачів:** `Demo1234!`

⚠️ Зверни увагу на регістр і знак оклику в кінці — пароль чутливий до регістру.

| Email                   | Роль           | Профіль                                                                                                 |
| ----------------------- | -------------- | ------------------------------------------------------------------------------------------------------- |
| `admin@demo.local`      | `ADMIN`        | Адміністратор платформи. Доступ до `/profile/admin` і модерації заявок.                                 |
| `org-rescue@demo.local` | `ORGANIZATION` | edrpou: 12345678 Власник «Rescue Львів» — зоозахисна організація. Може створювати події, збори, новини. |
| `org-eco@demo.local`    | `ORGANIZATION` | edrpou: 87654321 Власник «EcoKyiv» — екологічна організація.                                            |
| `vol-anna@demo.local`   | `VOLUNTEER`    | Анна Шимчук. Верифікована, 170 балів, рейтинг 4.80.                                                     |
| `vol-petro@demo.local`  | `VOLUNTEER`    | Петро Іваненко. Верифікований, 65 балів, член EcoKyiv.                                                  |
| `user@demo.local`       | `APP_USER`     | Марія Бойко. Звичайний користувач без волонтерського чи організаційного профілю.                        |

> Після `POST /auth/login/*` відповідь містить **два** токени: `accessToken` (15 хв, JWT) і `refreshToken` (7 днів, opaque hex). Фронт зберігає обидва в `localStorage` і автоматично оновлює access через `/auth/refresh` при 401. Повторний seed також очищає таблицю `refresh_token` — наявні логіни перестають працювати, треба перелогінитись.

---

## Що в базі після seed

| Сутність            | К-сть | Опис                                                                              |
| ------------------- | ----- | --------------------------------------------------------------------------------- |
| Локації             | 13    | Обласні центри України для подій, тікетів і профілів організацій                  |
| Категорії           | 19    | Тематичні напрями платформи: освіта, медицина, екологія, соціальна допомога тощо  |
| Користувачі         | 43    | Адмін, організації, волонтери, звичайні користувачі, pending/blocked демо-акаунти |
| Організації         | 19    | 16 VERIFIED, 2 PENDING, 1 REJECTED                                                |
| Проєкти/події       | 32    | Активні, чернетки та модераційні кейси для адмін-панелі                           |
| Реєстрації на події | ~57   | ACCEPTED / PENDING / REJECTED                                                     |
| Новини              | 30    | Опубліковані, закріплені, pending та rejected записи                              |
| Збори               | 16    | Активні та модераційні кейси з професійними описами потреб                        |
| Донати              | 28    | Іменні та анонімні внески з нейтральними повідомленнями                           |
| Approval requests   | many  | APPROVED + 10 PENDING + 5 REJECTED                                                |
| Tasks               | 27    | Мікрозавдання в межах подій                                                       |
| Rewards             | 13    | Нагороди платформи у стриманому професійному стилі                                |
| Points transactions | ~29   | Нарахування, списання, бонуси та корекції                                         |
| Membership requests | ~29   | REQUEST / INVITE у різних статусах                                                |
| Reports             | 10    | Фінансові, результатні й активнісні звіти організацій                             |

---

## Регенерація argon2 хешу

Якщо хочеш змінити пароль для демо-користувачів — згенеруй новий хеш через ту саму бібліотеку що в `auth.service.ts`:

```powershell
cd backend/server
node -e "require('argon2').hash('YourNewPassword').then(console.log)"
```

Скопіюй вивід (формат `$argon2id$v=19$m=65536...`) і встав замість поточного хешу в `prisma/seed.sql` (хеш використовується для всіх демо-користувачів — найпростіше зробити пошук і заміну).

---

## Швидкі перевірки після seed

```sql
-- Користувачі
SELECT email, role, status FROM app_user;

-- Організації
SELECT id, name, verification_status FROM organization_profile;

-- Проєкти + статус
SELECT id, title, status FROM project;

-- Збори + сума
SELECT id, title, current_amount, goal_amount, status FROM fundraising_campaign;
```
